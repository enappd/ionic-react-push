import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.enappd.ionreactfullapp3',
  appName: 'ionic-react-push',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
